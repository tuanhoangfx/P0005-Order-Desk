{
  "installCommand": "corepack enable && corepack pnpm install --frozen-lockfile false",
  "buildCommand": "corepack pnpm exec tsc --noEmit && corepack pnpm exec vite build",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
